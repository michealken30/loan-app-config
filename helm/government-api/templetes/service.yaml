apiVersion: v1
kind: Service
metadata:
  name: {{ include "government-api.fullname" . }}-service
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "government-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "government-api.selectorLabels" . | nindent 4 }}